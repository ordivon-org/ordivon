[tools]
node = "26.9.0"
python = "3.14.7"
uv = "0.12.16"
pnpm = "12.4.2"
shellcheck = "0.11.0"

[tasks.verify]
run = """
set -euo pipefail
pnpm install --frozen-lockfile
pnpm bootstrap:python
.venv/bin/ruff check .
pnpm check
git ls-files -z -- '*.sh' | xargs -0 -r shellcheck
"""

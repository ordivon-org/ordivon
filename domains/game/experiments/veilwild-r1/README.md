# Project Veilwild — Capability Activation R1

Status: `EXPERIMENTAL_COMPOSITE_PRESSURE_INSTANCE`

This directory is the integration surface for `docs/GAME_CAPABILITY_ACTIVATION_R1.md`.

It is deliberately separate from the registered Station Zero executable and from canonical G0–G8 product-stage authority.

## Authority boundary

```text
Veilwild R1 experiment
!= selected first Ordivon Game product
!= G0 admission
!= Station Zero replacement
```

The experiment asks whether one coherent 3D ecological stealth/observation slice can force previously weak capabilities into evidence-bearing production use.

## Frozen pre-run apparatus evidence

Workstation candidate freeze (separate repository/workspace):

```text
workstation commit: ab01a12
freeze document: docs/PRE_GAME_LOADOUT_FREEZE_R1.md
freeze SHA-256: 0f7a1a530280fb221d2205923aa06fb3618dd17cca5ec08cdd40fb8fb096f969
standing: READY_FOR_VEILWILD_R1 / FROZEN_FOR_COMPOSITE_PRESSURE
```

That evidence is an apparatus/precondition input only. It does not grant any Game capability standing.

## Integration directories

- `godot/` — the one integrated playable runtime candidate.
- `assets/environment/` — editable environment production sources and exports.
- `assets/creature/` — editable creature/rig/animation sources and exports.
- `assets/materials/` — material/lookdev sources and runtime exports.
- `assets/audio/` — source audio, processed assets, runtime-ready outputs.
- `research/` — research-to-product abstractions and exact source notes.
- `evidence/` — product/capability receipts tied to exact integrated conditions.
- `protocols/` — Human-playtest-ready protocol; no Human claims without participants.
- `build/` — generated build outputs; durable large artifacts may be ignored or regenerated as appropriate.

## Common product interface

Initial cross-lane creature state vocabulary:

```text
HIDDEN
ORIENTING
FORAGING
ALERT
FREEZE
EVADE
OBSERVABLE
```

This is a product-local interface, not a permanent global schema. Lanes D/E/F/G must converge on it or explicitly replace it in the integration owner branch.

Initial cue classes:

```text
VISUAL_BODY
VISUAL_MOTION
ENV_TRACE
SPATIAL_AUDIO
BEHAVIOR_CHANGE
OBSERVATION_CONFIRMATION
```

## Campaign law

```text
Game blocker
→ locate owning constituent E2E
→ repair capability/tool/process there when bounded and verifiable
→ validate constituent standing
→ replay the same Veilwild consequence
```

Do not reopen speculative preload work merely because a specialist tool exists.

# PC02-F0 — Persistent Workshop structural carrier

This is the smallest structural falsifier for **PC02 Persistent Workshop** after the PC01 carrier boundary was recorded.

It reuses the PGP-I creation grammar rather than rebuilding broad creation UX:

- primitives: `circle / square / diamond / bar / ring`;
- six-piece budget;
- the same position/rotation/scale transform vocabulary;
- explicit immutable committed revisions.

The carrier adds only PC02 composition couplings already required by the product authority: an explicit mechanical intent axis, persistent revision state, deterministic context profiles, build-path dependency through attachment sites derived from the exact committed artifact, and advisory multi-dimensional qualitative context response.

The intent axis is not decorative metadata: `spread=wide` versus `spread=compact` changes whether the same artifact is mechanically admissible for commit, independently of any audience profile. Context evaluation receives the committed artifact only, never the intent metadata or an unsaved working copy.

The deterministic three-round path intentionally rejects the `service_bay` preference to compress the artifact, preserves `spread=wide`, advances to Round 2, reloads persisted state from JSON, and uses an attachment affordance that exists only because Revision 1 was committed. A second reload recovers two immutable revisions before Round 3. The same artifact receives conflicting footprint observations from `service_bay` and `open_gallery`; there is no aggregate audience score/rank/reward/progression threshold.

Run:

```bash
python experiments/product-composition-r1/pc02-persistent-workshop-f0/acceptance.py
```

Expected standing:

```text
SURVIVES_PC02_F0_STRUCTURAL_COMPOSITION
mechanismLibraryModified=false
runtimeAgentProfile=none
```

The acceptance writes the structural receipt plus two persisted re-entry snapshots under `evidence/`.

This is structural evidence only. It does **not** establish Human authorship, expressive ownership, aesthetic value, desire to create, enjoyment, retention, market demand, product selection, or G0 admission.

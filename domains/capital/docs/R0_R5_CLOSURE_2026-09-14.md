# Market Capital R0–R5 bounded non-live closure — 2026-09-14

## Standing

`PASS_BOUNDED_NONLIVE_R0_R5_SEMANTIC_CLOSURE`

This closes the R0–R5 migration as a bounded, non-live capital-institution framework. It does **not** admit private account data or external financial writes.

## What is now closed

- **R0 semantic freeze:** the canonical eight retained semantics are active and fail closed.
- **R1 evidence substrate:** same-cut witness records materialize through Arrow/Parquet and replay through DuckDB without giving storage offsets authority.
- **R2 trading substrate:** pinned LEAN is available; NautilusTrader remains a bounded shadow candidate; FIX 4.4 projection passes without credentials or network sessions.
- **R3 capital substrate:** TigerBeetle 0.17.9 provides accounting/pending-transfer mechanics; deterministic account namespaces, reservation resolution, ordinary restart continuity and read-only terminal-history reconciliation are qualified.
- **R4 control composition:** mature capability providers remain external owners; Market Capital retains only proof/currentness/authority/reconciliation seams. Generic workflow/policy/observability systems cannot mint EffectAuthority.
- **R5 differential closure:** three frozen final Finance evidence fixtures replay the semantic outcomes that the clean-room architecture intentionally retained. No legacy Finance code is imported.

## Canonical verification entry points

```text
scripts/run-canonical-tests
scripts/run-r0-r5-closure --output evidence/r0-r5-closure-20260914.json
```

The canonical Python test environment composes the existing Ordivon Research Python capability with Market Capital's own project-local dependency environment rather than duplicating the research dependency stack.

## Remaining independent boundaries

These are not R0–R5 closure failures:

- private account observation remains `PENDING_FRESH_PROVIDER_VERIFICATION` / `NOT_ADMITTED`;
- `ExternalFinancialWriteAdmission` remains `NOT_ADMITTED`;
- the external-write verifier is `NOT_IMPLEMENTED` and no provider write capability is bound;
- multi-replica TigerBeetle disaster-recovery qualification is an Operations concern and was not claimed by the bounded R3.2 restart test.

The framework should now be consumed for research, portfolio/decision output, execution-intent projection, capital-state accounting and reconciliation rather than expanded with additional local subsystems by default.

#!/usr/bin/env bash
set -euo pipefail
repo="$(cd "$(dirname "$0")/.." && pwd)"
export PYTHONPATH="$repo/src${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m ordivon_capital.market.opa_policy --repo "$repo" --config "$repo/config/execution_policy.json" "$@"

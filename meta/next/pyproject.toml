[project]
name = "ordivon-next"
version = "0.0.0"
description = "Repository environment for Ordivon Next."
requires-python = "==3.14.7"
dependencies = [
  "ordivon-composition",
]

[dependency-groups]
test = [
  "jsonschema>=4.26,<5",
  "playwright>=1,<2",
  "pytest==9.1.1",
  "z3-solver>=4,<5",
]
quality = [
  "ruff==0.16.8",
]
security = [
  "bandit>=1.9.4",
  "pip-audit>=2.10.1",
]
test-analysis = [
  "coverage",
]
authority = [
  "check-jsonschema",
  "jsonschema>=4.26,<5",
]
reasoning = [
  "ortools",
  "pyshacl",
  "rdflib",
  "unified-planning",
  "up-pyperplan",
  "z3-solver>=4,<5",
]

[tool.uv.sources]
ordivon-composition = { path = "../../packages/composition" }

[tool.uv]
package = false
default-groups = ["test"]
required-version = "==0.12.3"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"

[tool.ruff]
target-version = "py314"
extend-exclude = ["experiments"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "UP"]

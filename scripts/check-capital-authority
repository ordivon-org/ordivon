#!/usr/bin/env bash
set -euo pipefail
repo="$(cd "$(dirname "$0")/.." && pwd)"
export PYTHONPATH="$repo/src${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m market_capital.authority --repo "$repo" --config "$repo/config/execution_authority.json" "$@"

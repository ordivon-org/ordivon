# Network v2 owns routed network-namespace consumers.
# WSL may reset this kernel-global switch across a VM restart. systemd-sysctl reapplies it
# at boot; consumer services must not restore it to 0 when they stop.
net.ipv4.ip_forward = 1

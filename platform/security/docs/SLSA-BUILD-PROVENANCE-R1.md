# SLSA Build Provenance R1

Date: 2026-09-12
Standing: `BUILD_L1_PROVENANCE_VERIFIED` for the local acceptance builder; no Build L2/L3 claim.

Security v2 now distinguishes two standard attestations:

- **Build Provenance** (`https://slsa.dev/provenance/v1`): produced by the actual build process and describing the artifact, exact source revision, build definition, builder and invocation metadata.
- **Verification Summary Attestation**: produced later by Security after verifying evidence/policy.

They are not interchangeable. Security must not reconstruct Build Provenance after the build from incomplete logs.

The R1 local builder is deliberately limited to a SLSA Build L1 acceptance. It builds an exact `git archive HEAD` using uv, emits provenance inside the same builder invocation, and supports independent artifact/source/builder/build-type verification. It makes no hosted-builder, signed-provenance, or hardened-build claim.

## Physical R1 acceptance

The committed clean revision `48be7b79c8d7fcc7c4e6ef6ec628d110b478726a` was materialized with `git archive HEAD` into a temporary source tree and built with uv `0.12.3` / Python `3.12.13`.

Observed output:

- wheel: `ordivon_security_v2-0.1.0-py3-none-any.whl`;
- wheel SHA-256: `81d4a8e55b8c0c511ff2a137d4b760b537638a25d6908a8835e05e88fbaa5c8b`;
- provenance SHA-256: `0ecd20c2a6988c3564a1b57e85ebc2e05bc22d47caed1aa222d2bac2714471e9`;
- provenance predicate: `https://slsa.dev/provenance/v1`;
- build type: `https://ordivon.local/security-v2/build-types/uv-python-wheel/v1`;
- source dependency: the exact Git commit above;
- verifier standing: `BUILD_PROVENANCE_VERIFIED`.

Two physical falsifiers failed closed as required: an artifact with the same filename but changed bytes was rejected for artifact digest mismatch, and the correct artifact paired with the wrong expected source revision was rejected for source-binding mismatch. The source repository remained clean after the build.

This establishes a local **SLSA Build L1-shaped provenance path**. It does not establish Build L2 because the provenance is not yet signed and generated by an independently authenticated hosted build platform; it does not establish Build L3 hardened-build guarantees.
